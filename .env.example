# SkillTrack Environment Configuration
SECRET_KEY=skilltrack_dev_secret_key_sih26135
FLASK_ENV=development
FLASK_DEBUG=True
PORT=5000

# Database Configuration
# Default: true for zero-friction hackathon testing (SQLite)
# Set to false to connect to a live MySQL instance
USE_SQLITE=true
SQLITE_DB_PATH=database/skilltrack.db

# MySQL Configuration (Active when USE_SQLITE=false)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=skilltrack_db

# Prototype Outcome Score Formula Weights (Sum = 100)
WEIGHT_EMPLOYMENT=30
WEIGHT_RETENTION=25
WEIGHT_RELEVANCE=20
WEIGHT_SALARY_GROWTH=15
WEIGHT_EMPLOYER_FEEDBACK=10
